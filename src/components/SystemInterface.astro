<div class="system-readout-block">
  <p class="system-readout-block__intro">
    Aurora no describe una decisión.
    La somete a cálculo estructural antes de ejecutarla.
  </p>

  <div class="system-readout" aria-label="Aurora system readout">
    <div class="system-readout__title">SYSTEM READOUT</div>

    <div class="system-readout__section">
      <p class="system-readout__label">variables activas</p>
      <p>costos</p>
      <p>demanda</p>
      <p>regulación</p>
      <p>timing</p>
    </div>

    <div class="system-readout__section">
      <p class="system-readout__label">presión estructural</p>
      <p class="system-readout__state">elevada</p>
    </div>

    <div class="system-readout__section">
      <p class="system-readout__label">estado de riesgo</p>
      <p class="system-readout__state">controlado</p>
    </div>

    <div class="system-readout__section">
      <p class="system-readout__label">huella</p>
      <p>decision_id: AUR-4931</p>
      <p>protocol: vΩ</p>
      <p>pressure point: día 32</p>
      <p>movement recorded</p>
    </div>
  </div>
</div>

<style>
  .system-readout-block {
    display: grid;
    gap: 1.5rem;
    justify-items: start;
  }

  .system-readout-block__intro {
    margin: 0;
    max-width: 30ch;
    color: var(--color-text);
    font-size: clamp(20px, 2.3vw, 24px);
    line-height: 1.34;
    letter-spacing: -0.025em;
  }

  .system-readout {
    display: grid;
    gap: 1.25rem;
    width: 100%;
    max-width: 32rem;
    padding: 0;
    background: transparent;
    border: 0;
    border-radius: 0;
    box-shadow: none;
    text-align: left;
  }

  .system-readout__title,
  .system-readout__label,
  .system-readout__section p {
    margin: 0;
    font-family: "DM Mono", monospace;
  }

  .system-readout__title {
    color: var(--color-text);
    font-size: 11px;
    line-height: 1.4;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .system-readout__section {
    display: grid;
    gap: 0.18rem;
  }

  .system-readout__label {
    margin-bottom: 0.22rem;
    color: rgba(247, 247, 245, 0.68);
    font-size: 11px;
    line-height: 1.5;
    letter-spacing: 0.08em;
    text-transform: lowercase;
  }

  .system-readout__section p {
    color: var(--color-text-secondary);
    font-size: 12px;
    line-height: 1.7;
    letter-spacing: 0.04em;
  }

  .system-readout__section .system-readout__state {
    color: var(--color-text);
  }

  @media (max-width: 768px) {
    .system-readout-block__intro {
      font-size: 20px;
    }
  }
</style>
