---
import "../styles/tokens.css";
import "../styles/global.css";

interface Props {
  title?: string;
  page?: "home" | "demo" | "default";
}

const { title = "Aurora", page = "default" } = Astro.props;
const showProductsToggle = page === "home";
const buildMarker =
  import.meta.env.VERCEL_GIT_COMMIT_SHA || import.meta.env.VERCEL_GITHUB_COMMIT_SHA || "local";
---
<!DOCTYPE html>
<html lang="es" data-build={buildMarker}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body data-proof="vomega-1" data-page={page} data-algorithm="core">
    <header class="site-header">
      <a href="/" class="logo-lockup" aria-label="Aurora">
        <img src="/aurora-logo.png" alt="" class="logo-symbol" width="22" height="22" loading="eager" decoding="async" />
        <span class="logo-wordmark">AURORA</span>
      </a>

      <nav class="site-nav" aria-label="Principal">
        {showProductsToggle ? (
          <button
            type="button"
            class="header-action"
            data-products-toggle
            aria-controls="products-menu"
            aria-expanded="false"
          >
            Productos
          </button>
        ) : (
          <a href="/#productos" class="header-action">Productos</a>
        )}
        <a href="/demo" class="header-action header-action-demo">Demo</a>
      </nav>
    </header>

    <slot />
  </body>
</html>
