#!/usr/bin/env bash
set -e

echo "Aurora Safety Net: pre-push check"

# repo limpio
if [ -n "$(git status --porcelain)" ]; then
  echo "❌ ERROR: repo sucio"
  exit 1
fi

# build obligatorio
echo "build test..."
pnpm -s build >/dev/null

echo "✔ build OK"
